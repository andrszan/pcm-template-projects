# PCM 模板工作区协作规则

## 语言规则

- 面向用户和维护者的文档、说明、注释等等默认使用中文。
- 文件名、命令、配置键、代码标识符、框架名和其他行业通用技术名词可以保留英文。
- 除非用户明确要求，不要自行把仓库说明改成英文。

## 仓库边界

本工作区包含多个相互独立的 Git 仓库。

- 上层仓库只管理文档、索引和辅助脚本。
- `repositories/pcm-frontend-templates` 是独立的前端模板仓库。
- `repositories/pcm-python-templates` 是独立的 Python 模板仓库。
- 上层仓库 `git status` 干净，不代表两个语言仓库也处于干净状态。
- 禁止在工作区根目录执行 `git clean -fdx` 或其他可能删除 ignored 内容的等价命令。
- 提交内容格式使用 Conventional Commits 规范。

修改文件前必须确认当前文件属于哪个仓库：

```bash
pwd
git rev-parse --show-toplevel
git status --short
```

除非任务明确需要，不要同时修改多个仓库。
- 除非任务明确要求使用 worktree，否则不要创建、进入或切换 worktree；需要并行隔离或用户明确要求时才使用。

## 架构选型

当用户要求推荐项目架构时：

1. 读取 `templates.yaml` 和 `docs/selection-guide.md`。
2. 根据需求判断是否需要前端、后端或其他独立项目。
3. 分别检查候选模板的 `template.yaml`；需要进一步比较时，再查看依赖声明和 README。
4. 独立选择能够满足各部分硬性要求的最简单模板，不绑定无关技术。
5. 没有合适的正式模板时必须明确说明，不得把临时建议描述成 PCM 正式模板。
6. 说明推荐理由、重要限制和派生项目需要验证的契约。
7. AI 只负责分析和建议，最终决定由用户作出。

## 项目维护

- 默认直接在语言仓库的 `main` 分支修改对应项目目录，不为每个项目维护长期开发分支。
- 需要独立评审、并行开发或隔离较大变更时，才创建短期分支；完成后合并回 `main`。
- 不得向基础项目加入未经任务确认的客户业务代码。
- 只有已通过项目契约、存在于语言仓库默认分支且登记在 `templates.yaml` 的项目起点，才是正式可选项目；不得登记仅存在于短期分支的目录。
- 涉及正式登记、重命名或删除时，遵循 `docs/template-contract.md` 的跨仓库顺序；各独立 Git 仓库分别检查、审查和提交。
